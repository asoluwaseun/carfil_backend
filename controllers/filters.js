"use strict"
const Models = require('../database/models/')

class Filters{
    static  async getFilters(req, res){
        try{

        }
        catch(err){
            res.sendStatus(500)
        }
    }
}

module.exports = Filters
